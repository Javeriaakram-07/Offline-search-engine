# Offline-search-engine
This project is a simple offline search engine built in C++ using structured programming. It loads multiple text files, supports exact and partial keyword search, saves history, handles no-result cases, and uses only arrays, pointers, dynamic memory, and file handling.
